NODE_ENV=development
PORT=3000

# API URL for server-side proxy (container-to-container)
API_URL=http://api:3001
# API URL for client-side requests (browser to host)
NEXT_PUBLIC_API_URL=http://localhost:3001